---
on:
  push:
    branches: master
  pull_request:
    branches: master

jobs:
  Windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
      - uses: ilammy/msvc-dev-cmd@v1
      - name: Build
        run: nmake
